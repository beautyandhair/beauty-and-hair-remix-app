query CartInput($productTags: [String!]!, $collections: [ID!]!, $excludedCollections: [ID!]) {
  cart {
    lines {
      id
      merchandise {
        __typename
        ... on ProductVariant {
          id
          metafield(namespace: "global", key: "CLEARANCE") {
            value
          }
          product {
            hasAnyTag(tags: $productTags)
            inAnyCollection(ids: $collections)
            inExcludedCollection: inAnyCollection(ids: $excludedCollections)
          }
        }
      }
      sellingPlanAllocation {
        sellingPlan {
          id
          name
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app:discount-exclude", key: "function-configuration") {
      value
    }
  }
}
